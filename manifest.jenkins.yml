applications:
- name: pz-sak
  host: pz-sak
  buildpack: 'https://github.com/cloudfoundry/staticfile-buildpack.git'
  memory: 256M
  disk_quota: 128M
  instances: 1
  timeout: 30
  services:
    - pz-geoserver-efs
